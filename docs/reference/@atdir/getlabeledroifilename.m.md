# getlabeledroifilename

```
  GETLABELEDROIFILENAME - get the image file from an ATDIR
 
   ROIFILENAME = GETLABELEDROIFILENAME(ATD, ITEMNAME)
 
   Returns the labeled ROI filename associated with the ROI item name
   ITEMNAME.

```
